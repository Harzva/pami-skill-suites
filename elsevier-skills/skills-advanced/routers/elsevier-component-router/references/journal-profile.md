# Router profile

This is not a single-journal profile. It is a router profile for the Elsevier Skills skill-suite.

The router coordinates independent journal adapters and reusable manuscript-component skills. It preserves publisher-family boundaries and uses official-source reminders instead of inventing policy rules.
