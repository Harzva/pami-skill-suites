# `ieee-baseline-selection` skill

Component skill for **baseline choice, reimplementation fairness, hyperparameter disclosure, and comparison scope** in IEEE journal manuscripts.

Use this folder as a complete skill unit. Copy the entire folder, not just `SKILL.md`, because the references, checklists, and examples are part of the execution context.

## Typical use

```text
@ieee-baseline-selection Rewrite this section for IEEE journal style and return a claim-evidence audit.
```
