# Context-Safe Architecture

The default `skills/` directory exposes only macro skills. Detailed journal, component, and submission rules live as static fragments under `ieee-skill-suite` and as optional independent skills under `skills-advanced/`.

Version: 0.7.0
