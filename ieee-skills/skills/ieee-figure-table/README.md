# ieee-figure-table

Default macro skill in the IEEE context-safe journal skill-suite.

This directory is intentionally small and visible by default. Detailed journal, component, and submission rules live in the suite static fragments or in `skills-advanced/` for optional independent installation.
