# Promotion Queue

Pack: `signal-processing`

L0 candidates can be promoted only through a staged draft workflow. Promotion never adds default top-level skills.

## L0 candidates

- `signal-processing-letters` — IEEE Signal Processing Letters — run `python scripts/promote_journal_candidate.py --pack signal-processing --journal-id signal-processing-letters` after collecting official links.
