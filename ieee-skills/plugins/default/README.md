# IEEE Skills Default Plugin

Default macro skill package for IEEE Skills v1.2.0.

Build the installable zip with:

```bash
python scripts/build_distribution.py
```


Expansion packs remain opt-in and are not default-visible.
